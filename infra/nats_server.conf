# minimal valid NATS config for JetStream (compatible with nats:2.10.6)
listen: "0.0.0.0:4222"
http_port: 8222
server_name: "beast-nats-dev"

jetstream {
  store_dir: /data/jetstream
  max_mem_store: 512Mb
  max_file_store: 10Gb
}
